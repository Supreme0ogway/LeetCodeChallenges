**************** return first occurrance of a string in another string and -1 if it doesn’t appear ***********************************************

class Solution {
    public int strStr(String haystack, String needle) {
        return haystack.indexOf(needle);
    }
}